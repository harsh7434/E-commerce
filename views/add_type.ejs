<%- include("header")%>


<main id="main" class="main">

    <div class="pagetitle">
      <h1>Add Type Data</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/admin/deshboard">Home</a></li>
          <li class="breadcrumb-item">Type</li>
          <li class="breadcrumb-item active">Add Type</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
      <div class="row">
        <div class="col-lg-10">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Type Form</h5>
              <!-- General Form Elements -->
              <form action="/admin/type/add_TypeData" method="post">
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-4 col-form-label">Select Category</label>
                  <div class="col-sm-8">
                    <select name="typecat_name" id="categoryID" onchange="return getSubcatData()" class="form-control">
                        <option value="0">Please select Category</option>
                        <% for(var ct of categoryData){%>
                            <option value="<%= ct.id%>"><%= ct.category_name%></option>
                        <%}%>
                    </select>
                  </div>
                </div>
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-4 col-form-label">Select Sub_Category</label>
                  <div class="col-sm-8">
                    <select name="typeSubCat_name" id="SubCatId" onchange="return getextraData()" class="form-control">
                        <option value="0">Please select Sub_Category</option>
                        <% for(var st of subCatData){%>
                            <option value="<%= st.id%>"><%= st.sub_category_name%></option>
                        <%}%>
                    </select>
                  </div>
                </div>
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-4 col-form-label">Select Extra_Category</label>
                  <div class="col-sm-8">
                    <select name="typeExtra_name" id="extraId" class="form-control">
                        <option value="0">Please select Extra_Category</option>
                        <% for(var et of extraData){%>
                            <option value="<%= et.id%>"><%= et.extra_category_name%></option>
                        <%}%>
                    </select>
                  </div>
                </div>
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-4 col-form-label">Enter Type</label>
                  <div class="col-sm-8">
                    <input type="text" name="type_name" placeholder="Enter Type" class="form-control">
                  </div>
                </div>
                <div class="row mb-3">
                  <div class="col-sm-10">
                    <button type="submit" class="btn btn-primary">Add Type</button>
                  </div>
                </div>
              </form>
            </div>
          </div>

        </div>
      </div>
    </section>

  </main><!-- End #main -->
<%- include("footer")%>